{
  "name": "cors-anywhere",
  "version": "0.4.4",
  "description": "CORS proxy for starnhl.com",
  "license": "MIT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha ./test/test*.js --reporter spec"
  },
  "dependencies": {
    "cors-anywhere": "^0.4.4",
    "http-proxy": "1.11.1",
    "proxy-from-env": "0.0.1"
  },
  "engines": {
    "node": "20.x"
  }
}
